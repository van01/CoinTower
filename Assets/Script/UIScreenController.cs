using UnityEngine;
using System.Collections;

public class UIScreenController : MonoBehaviour {

	// Use this for initialization
	void Start () {
		print(this.GetComponent<RectTransform>());
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
